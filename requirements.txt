# Web Scraping & Image Processing
Pillow
requests
beautifulsoup4
lxml

# Elasticsearch
elasticsearch>=8.0.0,<9.0.0

# Object Storage (MinIO / S3-compatible)
boto3>=1.34

# Selenium (Manufacturer Website Scraping)
selenium>=4.0
webdriver-manager

# Playwright (Tier 2 JS-heavy site rendering — run `playwright install chromium` after pip install)
playwright>=1.40
